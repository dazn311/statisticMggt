import React from 'react';

// import SvgIcon from '@material-ui/icons';
import Button from '@material-ui/core/Button';

import './homeIcon.styles.scss';
 
const HomeIcon = (props) => (<Button {...props}>
    Поиск
  </Button>)

export default HomeIcon;
   